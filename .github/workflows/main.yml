name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/indy_pool
    - name: Build docker image with indy pool inside
      run: Image was built
    - name: Run test
      run: echo "Not yet"
